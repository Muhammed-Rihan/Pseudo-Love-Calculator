<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pseudo Love Calculator</title>
    <style>
        /* General Styles */
body {
    font-family: 'Arial', sans-serif;
    background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
    color: #333;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    text-align: center;
}

.container {
    background: rgba(255, 255, 255, 0.9);
    padding: 2rem;
    border-radius: 15px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    max-width: 400px;
    width: 100%;
}

h1 {
    font-size: 2rem;
    margin-bottom: 1.5rem;
    color: #ff6f61;
}

/* Form Styles */
form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.input-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

label {
    font-size: 1rem;
    color: #555;
}

input {
    padding: 0.75rem;
    border: 1px solid #ccc;
    border-radius: 8px;
    font-size: 1rem;
    outline: none;
}

input:focus {
    border-color: #ff6f61;
    box-shadow: 0 0 5px rgba(255, 111, 97, 0.5);
}

button {
    padding: 0.75rem;
    background: #ff6f61;
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    cursor: pointer;
    transition: background 0.3s ease;
}

button:hover {
    background: #ff4a3d;
}

/* Result Page Styles */
.names {
    font-size: 1.5rem;
    color: #ff6f61;
    margin-bottom: 1rem;
}

.percentage h1 {
    font-size: 3rem;
    color: #ff6f61;
    margin: 1rem 0;
}

.quote {
    font-size: 1rem;
    color: #555;
    line-height: 1.5;
    margin-top: 1.5rem;
}

.quote p {
    margin: 0.5rem 0;
}
    </style>
</head>
<body>
    <div class="container">
        <h1>Pseudo Love Calculator</h1>
        <form action="/love" method="post">
            <div class="input-group">
                <label for="person1">Your Name:</label>
                <input type="text" name="person1" id="person1" placeholder="Enter your name">
            </div>
            <div class="input-group">
                <label for="person2">Partner's Name:</label>
                <input type="text" name="person2" id="person2" placeholder="Enter your partner's name">
            </div>
            <button type="submit">Calculate Love</button>
        </form>
    </div>
</body>
</html>